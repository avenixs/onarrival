const express = require("express");

const admin = require("../controllers/admin");

const router = express.Router();

// /users/add => GET
//router.get("/users/add", enterprise.getAddUserPage);

module.exports = router;