<%- include("../includes/head.ejs") %>
</head>

<body>
    <%- include("../includes/navigation-panel.ejs") %>
    
    <div class="main-panel">

        <%- include("../includes/left-bar.ejs") %>

        <div class="course-content">
            <h2 class="title-decor" id="vex-title">View Your Students</h2>
            <table class="table table-striped">
                <thead class="thead-dark">
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Name</th>
                        <th scope="col">Surname</th>
                        <th scope="col">Department</th>
                        <th scope="col">Email</th>
                        <th scope="col">Course</th>
                        <th scope="col">Disable</th>
                    </tr>
                </thead>
                <tbody class="table-with-ex">
            <% for(let i=0; i<students.length; i++) { %>
                    <tr class="<%- students[i].disabled ? 'disabled-row' : '' %>">
                        <td><%= eval(i+1) %></td>
                        <td><%= students[i].name %></td>
                        <td><%= students[i].surname %></td>
                        <td><%= students[i].nationality %></td>
                        <td><%= students[i].email %></td>
                        <td><%- students[i].Course==null ? "N/A" : students[i].Course.title %></td>
                        <% if(students[i].disabled) { %>
                        <td><button class="btn btn-success enable-user" value="<%= students[i].id %>">Enable</button></td>
                        <% } else { %>
                        <td><button class="btn btn-warning disable-user" value="<%= students[i].id %>">Disable</button></td>
                        <% } %>
                    </tr>
            <% }; %>
                </tbody>
            </table>
        </div>
    </div>

    <div id="window-cover"></div>

    <footer class="stick-bottom" id="page-footer">(c) Damian Wojtunik, Language on Arrival 2020</footer>

    <%- include("../includes/end.ejs") %>

    <script src="/scripts/view-students.js"></script>

</body>
</html>